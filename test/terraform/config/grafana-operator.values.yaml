commonLabels:
  dashboards: "grafana"

grafana:
  ingress:
    enabled: true
    host: "grafana.local"
    ingressClassName: "nginx"
  config:
    security:
      admin_password: "admin123"
    server:
      root_url: "https://grafana.local"